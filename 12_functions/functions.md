___
#### To run the program, make sure you are in the program's folder, then use the Run Command below.
___
##### Run Command:

`$ zig run -lc functions.c`

##### Results:

`1+2 = 3`
`1+2+3 = 6`
___
#### There are several other features to C functions. One is output parameters for returning multiple values, which we'll look at next.
