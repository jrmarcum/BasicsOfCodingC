___
#### To run the program, make sure you are in the program's folder, then use the Run Command below.
___
#### Note that arrays are printed element-by-element in C; there is no built-in array-to-string formatting.
___
##### Run Command:

`$ zig run -lc arrays.c`

##### Results:

`emp: [0 0 0 0 0]`
`set: [0 0 0 0 100]`
`get: 100`
`len: 5`
`dcl: [1 2 3 4 5]`
`2d:  [[0 1 2] [1 2 3]]`
