___
#### To run the program, make sure you are in the program's folder, then use the Run Command below.
___
#### C's `qsort()` accepts any comparison function, making custom sort criteria straightforward without needing a custom type.
___
##### Run Command:

`$ zig run -lc sorting-by-functions.c`

##### Results:

`[kiwi peach banana]`
