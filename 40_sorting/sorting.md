___
#### To run the program, make sure you are in the program's folder, then use the Run Command below.
___
#### C uses `qsort()` from `<stdlib.h>` with a comparison function for in-place sorting of any type.
___
##### Run Command:

`$ zig run -lc sorting.c`

##### Results:

`Strings: [a b c]`
`Ints:    [2 4 7]`
`Sorted:  true`
